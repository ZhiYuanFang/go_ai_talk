## 1. Implementation
- [x] 1.1 扩展语音对话请求模型，新增必填 `deviceNo` 字段，并完成控制器到服务层透传。
- [x] 1.2 在语音服务中实现设备级进程内会话缓存结构（按设备隔离、线程安全读写）。
- [x] 1.3 实现缓存策略：每设备最多保留最近 N 轮问答（默认 10），会话 TTL（默认 30 分钟），并支持配置覆盖。
- [x] 1.4 改造 DeepSeek 请求消息构造，按「system + history + current user」发送；成功后写回 assistant 回复。
- [x] 1.5 增加缓存保护策略（如最大设备会话数与淘汰/清理机制），避免长期运行内存无界增长。
- [x] 1.6 定义错误处理语义：DeepSeek 失败时不写入不完整轮次；无历史或历史过期时退化为单轮对话。
- [x] 1.7 增加最小对话文本长度校验（默认 2），并在调用 DeepSeek 前拦截过短文本；阈值由统一配置管理。

## 2. Validation
- [x] 2.1 单元测试覆盖：新设备首次对话、同设备连续多轮、不同设备隔离、TTL 过期、超出 N 轮裁剪。
- [x] 2.2 并发测试覆盖：同设备并发请求的一致性与线程安全。
- [x] 2.3 回归测试：原有音频校验、STT/TTS 流程与错误路径行为不回退。
- [x] 2.4 单元测试覆盖：文本长度小于阈值时直接返回错误，且不触发 DeepSeek 请求。
