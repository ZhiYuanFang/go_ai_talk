## 1. 枚举模型与适配基础

- [x] 1.1 为 `routing_key`、`target_type`、`mode`、状态机状态、`event type` 新增类型化枚举定义与中文注释。
- [x] 1.2 为各枚举实现 `Parse/Validate/String` 适配函数，统一错误语义。
- [x] 1.3 建立枚举集中注册入口，避免模块内重复裸字符串定义。

## 2. 路由键治理落地

- [x] 2.1 在 outbox 写入路径增加 `routing_key` 白名单校验，拒绝未注册值。
- [x] 2.2 在发布与消费链路统一使用 `RoutingKey` 枚举匹配，替换核心 `switch/HasPrefix` 裸字符串分支。
- [x] 2.3 增加非法路由键告警日志与观测指标（拒绝次数、来源模块）。

## 3. 业务枚举迁移

- [x] 3.1 将 voice 中 `target_type` 和 `mode` 关键分支迁移为枚举匹配。
- [x] 3.2 将状态机状态与 `event type` 关键分支迁移为枚举匹配并补齐默认分支保护。
- [x] 3.3 在 contracts/service 边界统一使用枚举适配入口，避免重复转换逻辑。

## 4. 兼容迁移与验收

- [x] 4.1 保留旧字符串入口兼容层并输出弃用提示，确保迁移期行为一致。
- [x] 4.2 增加迁移验收清单：核心模块不得新增裸字符串匹配。
- [x] 4.3 更新 runbook 与开发规范，明确新增枚举值与路由键的注册流程。
