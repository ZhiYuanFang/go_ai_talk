## 1. 缓存模型与基础能力

- [x] 1.1 在 `cachekit` 中补充读模型所需通用能力（版本键、批量读取、补丁原子操作封装与观测埋点）。
- [x] 1.2 定义 `history/action/event/user` 统一缓存 key 与版本键规范，并补充中文注释与使用约束。
- [x] 1.3 为缓存读写与补丁失败新增指标与结构化日志（命中率、回源率、补丁失败率、版本冲突率）。

## 2. 历史与元数据读模型接入

- [x] 2.1 为 history 查询链路实现 Redis 优先读取、miss 回源、回填缓存（覆盖 `ListHistory` 与 `GetLatestHistory`）。
- [x] 2.2 为 action/event/user 查询链路实现 Redis 优先读取与回源回填，统一通过领域缓存仓储访问。
- [x] 2.3 将语音链路中直接 DAO 读取 action/event/history 的路径收敛到读模型入口，避免绕过缓存。

## 3. 异步缓存投影更新

- [x] 3.1 定义并发布 `history/action/event/user` 写后缓存投影事件（包含实体标识、操作类型、版本号、事件ID）。
- [x] 3.2 实现低延迟消费者按事件对 Redis 执行增量补丁更新（add/update/delete），替代写后删缓存。
- [x] 3.3 实现幂等去重与乱序保护（事件ID去重、版本校验、旧事件丢弃与冲突计数）。

## 4. DeepSeek 上下文读取优化

- [x] 4.1 改造 DeepSeek 历史上下文构造流程，优先读取 Redis 历史读模型并保留回源兜底。
- [x] 4.2 对 DeepSeek 上下文读取增加命中/回源/降级指标，验证语音链路在 Redis 异常场景可用。
- [x] 4.3 对历史窗口语义进行一致性校验，确保 Redis 与权威源在最近 N 小时数据边界一致。

## 5. 修复、回滚与运行保障

- [x] 5.1 增加缓存投影失败重试与待修复机制，支持按实体重建缓存。
- [x] 5.2 提供缓存对账/修复任务（采样或全量）并验证最终一致性恢复路径。
- [x] 5.3 更新 runbook：部署顺序、回滚策略、故障排查与缓存重建流程。
