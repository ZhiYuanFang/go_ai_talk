## 1. 前缀注册与枚举收敛

- [x] 1.1 在 `eventkit` 新增路由键前缀常量（`history.record.`、`device.`、`voice.task.`）并补齐中文注释。
- [x] 1.2 将现有路由键枚举调整为“前缀 + 后缀”组合定义，消除重复前缀字面量。
- [x] 1.3 保持路由键白名单注册入口不变，确保改造后 `Parse/Validate` 行为一致。

## 2. 分发链路改造

- [x] 2.1 在 outbox 处理入口统一执行白名单校验，拒绝未注册路由键并输出来源日志。
- [x] 2.2 将投影分发核心分支从“枚举逐项匹配”替换为“前缀分组匹配”。
- [x] 2.3 为合法但未映射处理器的前缀补充默认保护分支与失败语义。

## 3. 周边链路与兼容迁移

- [x] 3.1 同步调整 repair/reconcile 等按域识别事件的路径，复用统一前缀入口。
- [x] 3.2 保留旧字符串兼容入口，在适配层输出弃用提示并集中转换。
- [x] 3.3 执行定向构建/测试，验证 history/device/voice 相关链路未回归。

## 4. 文档与验收治理

- [x] 4.1 更新 runbook，明确新增路由键的注册、分发、校验、回滚流程。
- [x] 4.2 补充迁移验收清单：核心模块不得新增 `routing_key` 裸字符串匹配。
- [x] 4.3 在 OpenSpec 任务与说明中记录“白名单校验 + 前缀分发”作为后续扩展基线。
