# =============================================================================
# 本机开发 Compose 环境变量示例（build + :local）。
#
# 生产 / 测试部署请改用分环境示例（从 registry pull，勿在本文件混用生产口令）：
#   manifest/docker/.env.prod.example  → 复制为 .env.prod，IMAGE_TAG=v1.0.0
#   manifest/docker/.env.test.example → 复制为 .env.test，IMAGE_TAG=v1.0.0-rc.1
# 详见 docs/runbooks/release-deploy-and-run.md §2.5–§2.8。
#
# 用法（本机开发）：
#   docker compose --env-file manifest/docker/.env.example \
#     -f manifest/docker/docker-compose.microservices.yml up -d --build
# 或复制为 manifest/docker/.env 再填写。
# 若各 LINK 留空，进程仍使用 manifest/config/config.*.yaml 里的 database.default.link / database.app.link
#（含公网占位 IP）。Compose 注入 *_DB_LINK 后，各 cmd/*/main.go 在启动时 gdb.SetConfigGroup 强制覆盖 yaml（见 internal/platform/dbcfg）。
# 修改 link 后若仍连旧地址，请对业务服务加 --force-recreate。
#
# 说明：compose 里 DSN 已用引号包裹插值，避免「写了 .env.example 仍连 120.x」的 YAML 截断问题。
# =============================================================================
#
# --- MySQL 与 Docker 在同一台宿主机（示例，请改口令）---
HISTORY_DB_LINK=mysql:root:Fang930927@tcp(host.docker.internal:3306)/ai_voice_history
DEVICE_DB_LINK=mysql:root:Fang930927@tcp(host.docker.internal:3306)/ai_voice_device
VOICE_DB_LINK=mysql:root:Fang930927@tcp(host.docker.internal:3306)/ai_voice_voice
# worker 专用库名 ai_voice_worker（仅含 domain_outbox 等 worker 域表；与 history 库分离后由 DBA 建库/迁移）
WORKER_OUTBOX_DB_LINK=mysql:root:Fang930927@tcp(host.docker.internal:3306)/ai_voice_worker
# gateway-app-server：版本检查读 ai_voice_app.version（对应 GF_DATABASE_APP_LINK）
APP_DB_LINK=mysql:root:Fang930927@tcp(host.docker.internal:3306)/ai_voice_app
UCG_DB_LINK=mysql:root:Fang930927@tcp(host.docker.internal:3306)/ai_voice_ucg
#
# --- 宿主机静态资源（Compose 默认 bind mount，与下列路径一致时无需设置）---
# 事件 logo：device-service 写 /ai_talk_images/ → 宿主机 /ai_talk_images
# App APK：gateway-app 写 /apk/ai_talk/ → 宿主机 /apk/ai_talk
# 若覆盖存储路径，环境变量须与 docker-compose 卷挂载点相同：
# DEVICE_EVENT_IMAGE_STORAGE_DIR=/ai_talk_images/
# GATEWAY_APP_APK_STORAGE_DIR=/apk/ai_talk/
#
# --- MySQL 在其它机器：把 tcp(...) 内主机改为 RDS / 内网 IP / 内网 DNS ---
#
# HISTORY_DB_LINK=
# DEVICE_DB_LINK=
# VOICE_DB_LINK=
# WORKER_OUTBOX_DB_LINK=
# APP_DB_LINK=
# UCG_DB_LINK=
