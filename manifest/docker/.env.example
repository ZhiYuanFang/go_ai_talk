# 复制本文件为同目录下的 `.env` 后填写真实值，切勿将含口令的 `.env` 提交到 git。
# 启动示例（在仓库根目录）：
#   docker compose --env-file manifest/docker/.env -f manifest/docker/docker-compose.microservices.yml up -d --build
#
# --- MySQL 与 Docker 在同一台宿主机 ---
# DSN 中主机请使用 host.docker.internal（compose 已为业务服务配置 extra_hosts: host-gateway），
# 避免容器内访问宿主机「公网 IP」时的发夹 NAT / connection refused。
# 示例：
# HISTORY_DB_LINK=mysql:root:YOUR_PASSWORD@tcp(host.docker.internal:3306)/ai_voice_history
# DEVICE_DB_LINK=mysql:root:YOUR_PASSWORD@tcp(host.docker.internal:3306)/ai_voice_device
# VOICE_DB_LINK=mysql:root:YOUR_PASSWORD@tcp(host.docker.internal:3306)/ai_voice_voice
# WORKER_DB_LINK=mysql:root:YOUR_PASSWORD@tcp(host.docker.internal:3306)/ai_voice_history
#
# --- MySQL 在其它机器（RDS、另一台 VM、同 VPC 数据库等）---
# 将 tcp(...) 内主机改为从容器内可达的 DNS 或内网 IP 即可；不必使用 host.docker.internal。
#
# 若以下变量留空，各服务仍使用 manifest/config/config.*.yaml 中的 database.default.link 占位值。

HISTORY_DB_LINK=mysql:root:Fang930927@tcp(host.docker.internal:3306)/ai_voice_history
DEVICE_DB_LINK=mysql:root:Fang930927@tcp(host.docker.internal:3306)/ai_voice_device
VOICE_DB_LINK=mysql:root:Fang930927@tcp(host.docker.internal:3306)/ai_voice_voice
WORKER_DB_LINK=mysql:root:Fang930927@tcp(host.docker.internal:3306)/ai_voice_history
