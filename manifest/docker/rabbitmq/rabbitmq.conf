# RabbitMQ 容器 stdout 日志级别（挂载至 /etc/rabbitmq/rabbitmq.conf）。
# 默认 info 下连接/channel 噪音极大；warning 仍保留 alarm 与认证失败。
# 排障时可临时改为 info 并 force-recreate 容器。
log.console = true
log.console.level = warning
log.connection.level = warning
log.channel.level = warning
