# 主网关 gateway-service（:9701）：无业务库。
# Redis：Compose 注入 GF_REDIS_DEFAULT_ADDRESS（见 manifest/docker/.env.*）。
server:
  address:     ":9701"
  openapiPath: "/api.json"
  swaggerPath: "/swagger"

logger:
  level : "all"
  stdout: true
