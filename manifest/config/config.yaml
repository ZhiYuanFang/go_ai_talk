server:
  address:     ":9701"
  openapiPath: "/api.json"
  swaggerPath: "/swagger"

logger:
  level : "all"
  stdout: true

redis:
  default:
    # 运行时请通过环境变量或挂载配置覆盖该占位值
    address: "redis-node-1:7001"
    db: 0
