server:
  address:     ":9701"
  openapiPath: "/api.json"
  swaggerPath: "/swagger"

logger:
  level : "all"
  stdout: true

redis:
  default:
    # Redis Cluster：三主种子逗号分隔（与 docker-compose.redis-cluster 拓扑一致），供 gredis 发现全集群。
    address: "redis-node-1:7001,redis-node-2:7002,redis-node-3:7003"
    db: 0
