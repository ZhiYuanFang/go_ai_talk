server:
  address: ":9801"
  openapiPath: "/api.json"
  swaggerPath: "/swagger"

logger:
  level: "all"
  stdout: true

database:
  default:
    # 运行时请通过环境变量或挂载配置覆盖该占位值
    link: "mysql:user:password@tcp(127.0.0.1:3306)/ai_voice_history"
    debug: false
